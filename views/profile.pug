extends layout

block content
  .timeline
    .followings.half
      h2 임시1
      if user.Followings
        for following in user.Followings
          div= following.nick
    .followers.half
      h2 임시2
      if user.Followers
        for follower in user.Followers
          div= follower.nick
